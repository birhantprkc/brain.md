#!/bin/sh
# brain.md uninstaller — thin wrapper around the Node uninstaller.
# Prefer: brain uninstall  or  npx brain-md uninstall
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
exec node "$SCRIPT_DIR/bin/brain.mjs" uninstall "$@"
